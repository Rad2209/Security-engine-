# Copy this file to .env and adjust as needed.

# Local dev default — points at the backend running via `npm start`/`npm run dev`.
# In production (Vercel), set this to your deployed Render backend URL instead,
# e.g. VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=http://localhost:5000/api